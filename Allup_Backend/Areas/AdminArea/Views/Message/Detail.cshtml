@model BillingAddress
<div class="container">
    <div class="row">
        <div class="col-6">
            <p>@Model.User.UserName</p>
            <p>@Model.Subject</p>
            <p>@Model.Text</p>
        </div>

        <div class="col-6">
            <a asp-controller="" asp-action="Index" class="btn btn-warning" style="color:white">Go Back</a>
        </div>
    </div>
</div>
